name: gtd

env:
  DJANGO_SETTINGS_MODULE: settings

requirements:
  pypm:
    - psycopg2
    - django
    - south

services:
  postgresql: postgresql-gtd

ignores: 
  - .git
  - dev.db

min_version:
  server: 0.9.0.143

